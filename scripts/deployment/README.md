# Deployment Scripts

## TODO
- [ ] Sysmon auto-install + config (PowerShell)
- [ ] Winlogbeat auto-install + config (PowerShell)
- [ ] Filebeat auto-install + config (Bash)
- [ ] ELK index template setup
- [ ] pfSense rule export/import
