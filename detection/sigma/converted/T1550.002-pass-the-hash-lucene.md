# T1550.002 — Pass-the-Hash | Sigma → Lucene Conversion

> **Sigma rule:** `detection/sigma/T1550.002-pass-the-hash.yml`  
> **Converted:** 2026-08-25  
> **Tool:** `sigma-cli` with `pysigma-backend-elasticsearch` v2.1.1  
> **Profile:** `ecs_windows`  
> **Target:** `lucene`  
> **Validated:** ✅ Returns 40 events in `winlogbeat-2026.08.25` — confirmed in Kibana  

---

## Conversion Command

```powershell
sigma convert -t lucene -p ecs_windows detection/sigma/T1550.002-pass-the-hash.yml
```

## Output (Lucene Query)

```
winlog.channel:Security AND ((event.code:4624 AND winlog.event_data.LogonType:3 AND winlog.event_data.AuthenticationPackageName:NTLM) AND (NOT (source.ip:(*127.0.0.1* OR *\:\:1* OR *\-*))) AND (NOT winlog.event_data.TargetUserName:*$))
```

---

## Use in Kibana

Paste the Lucene query above into **Kibana Discover** with:
- **Data view:** `winlogbeat-*`
- **Query language:** Lucene (switch from KQL in the top-right of the search bar)

Or use the equivalent KQL version directly:

```kql
event.code: "4624"
AND winlog.event_data.LogonType: "3"
AND winlog.event_data.AuthenticationPackageName: "NTLM"
AND NOT winlog.event_data.IpAddress: ("127.0.0.1" OR "::1" OR "-")
AND NOT winlog.event_data.TargetUserName: *$
```

---

## Kibana Alert Rule

| Field | Value |
|-------|-------|
| Name | `[INC-004] Pass-the-Hash — EID 4624 NTLM LogonType 3` |
| Rule type | Elasticsearch query |
| Index | `winlogbeat-*` |
| Query | Lucene output above |
| Threshold | `> 0` |
| Check every | `5 minutes` |
| Level | High |

---

## Validation Result

| Check | Result |
|-------|--------|
| sigma-cli parse | ✅ 100% — no errors |
| Kibana KQL test | ✅ 40 events returned — `winlogbeat-2026.08.25` |
| False positive filters applied | ✅ loopback IPs + machine accounts excluded |
| MITRE tag | ✅ `attack.lateral_movement` + `attack.t1550.002` |
